# Flask Configuration
FLASK_KEY=your-secret-key-here-change-this-in-production

# Database Configuration
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=5432

# For production (e.g., Heroku, Railway), you may get DATABASE_URL instead
# DATABASE_URL=postgresql://user:password@host:port/dbname
