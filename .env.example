# Flask Configuration
FLASK_KEY=your-secret-key-here-change-this-in-production

# Database Configuration (Option 1: Individual variables)
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=5432

# Database Configuration (Option 2: Single URL - Used by Heroku, Railway, Render)
# If DATABASE_URL is set, it will be used instead of the individual variables above
# DATABASE_URL=postgresql://user:password@host:port/dbname
